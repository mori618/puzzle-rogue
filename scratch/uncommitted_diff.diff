diff --git a/src/engine/PuzzleEngine.js b/src/engine/PuzzleEngine.js
index 32457c4..99e25b6 100644
--- a/src/engine/PuzzleEngine.js
+++ b/src/engine/PuzzleEngine.js
@@ -45,6 +45,9 @@ class PuzzleEngine {
     this.processing = false;
     this.currentCombo = 0;
     this.noSkyfall = false;
+    this.gravityDirection = 'down'; // 重力方向（'down' または 'up'）
+    this.hasOneStrokeSeal = false;  // 一筆書きの誓約が有効か
+    this.oneStrokeVisited = null;   // ドラッグ中の訪問済みセル（Set で管理）
     this.spawnWeights = {};
     this.types.forEach((t) => (this.spawnWeights[t] = 1));
     this.timerProgress = 1; // Added for external display
@@ -474,15 +477,18 @@ class PuzzleEngine {
     this.container.appendChild(el);
 
     if (isNew) {
-      // 新規オーブ: 盤面外(上方)からの落下アニメーション
-      const offsetY = -((startRowOffset + 1) * (this.orbSize + this.gap));
-      // transitionなしで上方にセット
+      // 重力方向に応じて盤面外からの落下アニメーションを設定
+      // 'up': 下方から上へ落ちる（正の Y オフセットから開始）
+      // 'down': 上方から下へ落ちる（負の Y オフセットから開始）
+      const fallSign = this.gravityDirection === 'up' ? 1 : -1;
+      const offsetY = fallSign * ((startRowOffset + 1) * (this.orbSize + this.gap));
       el.style.transition = 'none';
       el.style.transform = `translate3d(0, ${offsetY}px, 0)`;
       el.classList.add('orb-falling');
       orb.currentDx = 0;
       orb.currentDy = offsetY;
     } else {
+      el.style.transform = 'translate3d(0px, 0px, 0)'; // 初期状態の位置を明示的にインラインtransformで設定（ブラウザの描画バグ対策）
       orb.currentDx = 0;
       orb.currentDy = 0;
     }
@@ -535,23 +541,57 @@ class PuzzleEngine {
             orb._fallingTimeout = timeoutId;
             
             orb.el.classList.add('orb-falling');
+
+            orb.el.style.transform = `translate3d(${dx}px, ${dy}px, 0)`;
+            orb.currentDx = dx;
+            orb.currentDy = dy;
           } else {
-            orb.el.style.transition = '';
-            if (animClass) {
-              orb.el.classList.add(animClass);
+            // 位置が実際に変わった場合のみスタイル更新処理を実行（不要な再描画を防ぐ）
+            if (dx !== orb.currentDx || dy !== orb.currentDy) {
+              // 通常のドラッグ操作中の入れ替えアニメーションのクラス制御
+              if (!orb.el.classList.contains('orb-returning') && !orb.el.classList.contains('orb-falling')) {
+                orb.el.classList.add('orb-swapping');
+                
+                // 既存の入れ替えクリーンアップ用タイマーがあればクリア
+                if (orb._swappingTimeout) {
+                  clearTimeout(orb._swappingTimeout);
+                  this.activeTimeouts.delete(orb._swappingTimeout);
+                }
+                
+                // 150ms後にクラスを取り除く（cssのtransition時間120msより少し長めにして、スナップの途切れを防ぐ）
+                const timeoutId = setTimeout(() => {
+                  this.activeTimeouts.delete(timeoutId);
+                  if (orb && orb.el && !this._isDestroyed) {
+                    orb.el.classList.remove('orb-swapping');
+                  }
+                  orb._swappingTimeout = null;
+                }, 150);
+                
+                this.activeTimeouts.add(timeoutId);
+                orb._swappingTimeout = timeoutId;
+              }
+
+              if (animClass) {
+                orb.el.classList.add(animClass);
+              } else {
+                orb.el.classList.remove('orb-falling');
+                if (orb._fallingTimeout) {
+                  clearTimeout(orb._fallingTimeout);
+                  this.activeTimeouts.delete(orb._fallingTimeout);
+                  orb._fallingTimeout = null;
+                }
+              }
+
+              orb.el.style.transform = `translate3d(${dx}px, ${dy}px, 0)`;
+              orb.currentDx = dx;
+              orb.currentDy = dy;
             } else {
-              orb.el.classList.remove('orb-falling');
-              if (orb._fallingTimeout) {
-                clearTimeout(orb._fallingTimeout);
-                this.activeTimeouts.delete(orb._fallingTimeout);
-                orb._fallingTimeout = null;
+              // 位置が変わっていないが、animClassが指定された場合はクラスの付与等のみ行う
+              if (animClass) {
+                orb.el.classList.add(animClass);
               }
             }
           }
-
-          orb.el.style.transform = `translate3d(${dx}px, ${dy}px, 0)`;
-          orb.currentDx = dx;
-          orb.currentDy = dy;
           orb.r = r;
           orb.c = c;
         }
@@ -570,14 +610,20 @@ class PuzzleEngine {
       row.forEach(orb => {
         if (orb) {
           orb.isSkyfall = false;
-          // 残留している可能性のある落下用クラスとスタイルを完全にクリーンアップする
+          // 残留している可能性のある落下・入れ替え用クラスとスタイルを完全にクリーンアップする
           orb.el.classList.remove('orb-falling');
+          orb.el.classList.remove('orb-swapping');
           orb.el.style.transition = '';
           if (orb._fallingTimeout) {
             clearTimeout(orb._fallingTimeout);
             this.activeTimeouts.delete(orb._fallingTimeout);
             orb._fallingTimeout = null;
           }
+          if (orb._swappingTimeout) {
+            clearTimeout(orb._swappingTimeout);
+            this.activeTimeouts.delete(orb._swappingTimeout);
+            orb._swappingTimeout = null;
+          }
           if (orb.isMoveDrop) {
             orb.moveCount = 0;
             orb.moveSteps = 0;
@@ -603,6 +649,17 @@ class PuzzleEngine {
     this.dragging.el.classList.add("orb-grabbing");
     this.dragging.el.style.zIndex = "100";
 
+    // 一筆書きの誓約: ドラッグ開始時に訪問済みセットを初期化
+    if (this.hasOneStrokeSeal) {
+      this.oneStrokeVisited = new Set();
+      this.oneStrokeVisited.add(`${target.r},${target.c}`);
+      this._createOneStrokeIndicator(target.r, target.c);
+    }
+
+    if (this.container) {
+      this.container.classList.add("board-dragging-active");
+    }
+
     if (this.comboEl) this.comboEl.style.display = "none";
 
     this.moveStart = null;
@@ -659,6 +716,16 @@ class PuzzleEngine {
             this.timerId = setInterval(this.updateTimer, 20);
           }
 
+          // 一筆書きの誓約: 通過済みセルへの移動をブロック
+          if (this.hasOneStrokeSeal && this.oneStrokeVisited) {
+            const visitKey = `${nr},${nc}`;
+            if (this.oneStrokeVisited.has(visitKey)) {
+              return; // このマスは既に通過済みなので移動を拒否
+            }
+            this.oneStrokeVisited.add(visitKey);
+            this._createOneStrokeIndicator(nr, nc);
+          }
+
           const target = this.state[nr][nc];
           this.state[nr][nc] = this.dragging;
           this.state[this.dragging.r][this.dragging.c] = target;
@@ -671,13 +738,6 @@ class PuzzleEngine {
           this._incrementMoveDropCount(this.dragging);
           if (target) this._incrementMoveDropCount(target);
 
-          // 入れ替わるオーブにスワップアニメーションクラスを付与
-          if (target && target.el) {
-            target.el.classList.add('orb-swapping');
-            setTimeout(() => {
-              if (target && target.el) target.el.classList.remove('orb-swapping');
-            }, 160);
-          }
           soundManager.playSE(SE_IDS.DRAG_MOVE);
           this.render(); // Update positions
         }
@@ -753,32 +813,60 @@ class PuzzleEngine {
     }
     this._lastMovePoint = null;
 
-    this.dragging.el.classList.remove("orb-grabbing");
-    this.dragging.el.style.zIndex = "";
+    // --- 操作時間の計測と記録 ---
+    if (this.moveStart) {
+      const elapsed = Date.now() - this.moveStart;
+      this.totalMoveTimeRef.current += elapsed;
+    }
+
+    const target = this.dragging;
+    const hasMoved = !!this.moveStart;
+
+    // ドラッグしていたオーブを元のマスの位置に滑らかにスナップさせる
+    target.el.classList.remove("orb-grabbing");
+    target.el.classList.add("orb-returning");
+    
+    const targetTop = (target.r * (this.orbSize + this.gap));
+    const targetLeft = (target.c * (this.orbSize + this.gap));
+    const dx = targetLeft - target.baseLeft;
+    const dy = targetTop - target.baseTop;
+    
+    target.el.style.transition = 'transform 120ms cubic-bezier(0.25, 1, 0.5, 1)';
+    target.el.style.transform = `translate3d(${dx}px, ${dy}px, 0)`;
+    target.el.style.zIndex = "";
+    
     this.dragging = null;
 
+    if (this.container) {
+      this.container.classList.remove("board-dragging-active");
+    }
+
     window.removeEventListener("mousemove", this.onMove);
     window.removeEventListener("mouseup", this.onEnd);
     window.removeEventListener("touchmove", this.onMove);
     window.removeEventListener("touchend", this.onEnd);
 
-    // --- 操作時間の計測と記録 ---
-    if (this.moveStart) {
-      const elapsed = Date.now() - this.moveStart;
-      this.totalMoveTimeRef.current += elapsed;
-    }
+    // 一筆書きインジケーターのクリーンアップ
+    this._clearOneStrokeIndicators();
+    this.oneStrokeVisited = null;
 
     // ドラッグ終了時に盤面全体の transition/クラスを一旦リセット
     this.state.forEach(row => {
       row.forEach(orb => {
-        if (orb) {
+        if (orb && orb !== target) {
           orb.el.classList.remove('orb-falling');
+          orb.el.classList.remove('orb-swapping');
           orb.el.style.transition = '';
           if (orb._fallingTimeout) {
             clearTimeout(orb._fallingTimeout);
             this.activeTimeouts.delete(orb._fallingTimeout);
             orb._fallingTimeout = null;
           }
+          if (orb._swappingTimeout) {
+            clearTimeout(orb._swappingTimeout);
+            this.activeTimeouts.delete(orb._swappingTimeout);
+            orb._swappingTimeout = null;
+          }
         }
       });
     });
@@ -787,6 +875,13 @@ class PuzzleEngine {
 
     // クロノス・ストップ中はprocess()に進まない
     if (this.chronosStopActive) {
+      // スナップアニメーションのクラスのクリーンアップはクロノス・ストップ中であっても行う
+      setTimeout(() => {
+        if (target && target.el) {
+          target.el.classList.remove("orb-returning");
+          target.el.style.transition = '';
+        }
+      }, 120);
       return;
     }
 
@@ -794,13 +889,23 @@ class PuzzleEngine {
     this.isPointerDown = false;
     this.updateFastForwardState();
 
-    // 修正: 動かしていない（スワップしていない）場合はターンを進めない
-    // moveStart はスワップが発生した時点でセットされる
-    if (!this.moveStart) {
-      return;
-    }
-
-    this.process();
+    // スナップアニメーションの完了を待ってから、必要に応じて process() を実行する
+    setTimeout(() => {
+      if (target && target.el) {
+        target.el.classList.remove("orb-returning");
+        target.el.style.transition = '';
+      }
+      
+      // 修正: 動かしていない（スワップしていない）場合はターンを進めない
+      // hasMoved はスワップが発生した時点でセットされる
+      if (!hasMoved) {
+        return;
+      }
+      
+      if (!this._isDestroyed) {
+        this.process();
+      }
+    }, 120);
   }
 
   setSpawnWeights(weights) {
@@ -2072,19 +2177,37 @@ class PuzzleEngine {
 
   // 重力処理のみ（新規オーブ生成なし）- noSkyfall時に使用
   async gravityOnly() {
-    for (let c = 0; c < this.cols; c++) {
-      let emptySlots = 0;
-      for (let r = this.rows - 1; r >= 0; r--) {
-        if (this.state[r][c] === null) {
-          emptySlots++;
-        } else if (emptySlots > 0) {
-          const orb = this.state[r][c];
-          this.state[r + emptySlots][c] = orb;
-          this.state[r][c] = null;
-          orb.r = r + emptySlots;
+    if (this.gravityDirection === 'up') {
+      // 上方向重力: オーブを上へ詰める
+      for (let c = 0; c < this.cols; c++) {
+        let emptySlots = 0;
+        for (let r = 0; r < this.rows; r++) {
+          if (this.state[r][c] === null) {
+            emptySlots++;
+          } else if (emptySlots > 0) {
+            const orb = this.state[r][c];
+            this.state[r - emptySlots][c] = orb;
+            this.state[r][c] = null;
+            orb.r = r - emptySlots;
+          }
+        }
+      }
+    } else {
+      // 下方向重力（デフォルト）
+      for (let c = 0; c < this.cols; c++) {
+        let emptySlots = 0;
+        for (let r = this.rows - 1; r >= 0; r--) {
+          if (this.state[r][c] === null) {
+            emptySlots++;
+          } else if (emptySlots > 0) {
+            const orb = this.state[r][c];
+            this.state[r + emptySlots][c] = orb;
+            this.state[r][c] = null;
+            orb.r = r + emptySlots;
+          }
         }
+        // noSkyfall: 空きスロットは null のまま（新規オーブを生成しない）
       }
-      // noSkyfall: 空きスロットは null のまま（新規オーブを生成しない）
     }
     // 強制的にリフローを発生させ、既存のstyle変更をブラウザに認識させる
     void this.container.offsetHeight;
@@ -2096,20 +2219,43 @@ class PuzzleEngine {
   }
 
   async simultaneousGravity() {
-    for (let c = 0; c < this.cols; c++) {
-      let emptySlots = 0;
-      for (let r = this.rows - 1; r >= 0; r--) {
-        if (this.state[r][c] === null) {
-          emptySlots++;
-        } else if (emptySlots > 0) {
-          const orb = this.state[r][c];
-          this.state[r + emptySlots][c] = orb;
-          this.state[r][c] = null;
-          orb.r = r + emptySlots;
+    if (this.gravityDirection === 'up') {
+      // 上方向重力: オーブを上へ詰め、下端から新規オーブを生成
+      for (let c = 0; c < this.cols; c++) {
+        let emptySlots = 0;
+        for (let r = 0; r < this.rows; r++) {
+          if (this.state[r][c] === null) {
+            emptySlots++;
+          } else if (emptySlots > 0) {
+            const orb = this.state[r][c];
+            this.state[r - emptySlots][c] = orb;
+            this.state[r][c] = null;
+            orb.r = r - emptySlots;
+          }
+        }
+        // 下端（rows-1 から上方向）に空きスロット分の新規オーブを生成
+        for (let i = 0; i < emptySlots; i++) {
+          const targetRow = this.rows - 1 - i;
+          this.spawnOrb(targetRow, c, true, emptySlots - 1 - i);
         }
       }
-      for (let i = 0; i < emptySlots; i++) {
-        this.spawnOrb(i, c, true, emptySlots - 1 - i);
+    } else {
+      // 下方向重力（デフォルト）
+      for (let c = 0; c < this.cols; c++) {
+        let emptySlots = 0;
+        for (let r = this.rows - 1; r >= 0; r--) {
+          if (this.state[r][c] === null) {
+            emptySlots++;
+          } else if (emptySlots > 0) {
+            const orb = this.state[r][c];
+            this.state[r + emptySlots][c] = orb;
+            this.state[r][c] = null;
+            orb.r = r + emptySlots;
+          }
+        }
+        for (let i = 0; i < emptySlots; i++) {
+          this.spawnOrb(i, c, true, emptySlots - 1 - i);
+        }
       }
     }
     // 強制的にリフローを発生させ、初期のtransform位置をブラウザに確実に認識させる
@@ -2121,6 +2267,27 @@ class PuzzleEngine {
     this.render('orb-falling');
   }
 
+  /** 一筆書きの誓約: 通過セルのビジュアルインジケーターを生成 */
+  _createOneStrokeIndicator(r, c) {
+    const indicator = document.createElement('div');
+    indicator.className = 'one-stroke-tile';
+    // オーブと同じサイズ・位置に絶対配置
+    const baseTop = r * (this.orbSize + this.gap);
+    const baseLeft = c * (this.orbSize + this.gap);
+    indicator.style.width = `${this.orbSize}px`;
+    indicator.style.height = `${this.orbSize}px`;
+    indicator.style.top = `${baseTop}px`;
+    indicator.style.left = `${baseLeft}px`;
+    this.container.appendChild(indicator);
+  }
+
+  /** 一筆書きの誓約: 全インジケーターを削除 */
+  _clearOneStrokeIndicators() {
+    if (!this.container) return;
+    const indicators = this.container.querySelectorAll('.one-stroke-tile');
+    indicators.forEach(el => el.remove());
+  }
+
   // --- 単色全消しチェック（ヘルパー） ---
   // 盤面が空 かつ このイテレーションで消えた色がちょうど1種類なら +20コンボ
   async _checkMonoClear(iterationErasedColors) {
diff --git a/src/index.css b/src/index.css
index 456953d..c3c958a 100644
--- a/src/index.css
+++ b/src/index.css
@@ -148,9 +148,8 @@ body {
   z-index: 10;
   /* GPUアクセラレーション: transformベースでアニメーション */
   will-change: transform, opacity;
-  transition:
-    transform 0.35s cubic-bezier(0.25, 1, 0.5, 1),
-    opacity 0.3s ease;
+  /* デフォルトでは transform の transition は設定せず、他のアニメーションとの競合やバグを防止 */
+  transition: opacity 0.3s ease;
   /* top/leftは初期配置用。アニメーションはtranslate3dで行う */
   transform: translate3d(0, 0, 0);
   /* overflow: hidden; Removed to allow badge to protrude */
@@ -159,7 +158,6 @@ body {
 /* 形状定義（完全丸み対応版：border-radiusのみ使用） */
 .orb-shape-fire {
   border-radius: 50% 50% 50% 0%;
-  transform: rotate(45deg);
 }
 
 /* しずく（斜め） */
@@ -180,7 +178,6 @@ body {
 /* 有機的オーバル */
 .orb-shape-dark {
   border-radius: 5% 50% 50% 50%;
-  transform: rotate(-45deg);
 }
 
 /* 雫（逆向き斜め） */
@@ -191,6 +188,7 @@ body {
 /* 親の形状に合わせて inner も整形する（overflow: hidden なしの対策） */
 .orb.orb-shape-fire .orb-inner {
   border-radius: 10% 50% 50% 50%;
+  transform: rotate(45deg);
 }
 
 .orb.orb-shape-water .orb-inner {
@@ -207,6 +205,7 @@ body {
 
 .orb.orb-shape-dark .orb-inner {
   border-radius: 50% 50% 50% 10%;
+  transform: rotate(-45deg);
 }
 
 .orb.orb-shape-heart .orb-inner {
@@ -225,8 +224,12 @@ body {
 }
 
 /* 入れ替え時のスナップ感を出すための短いtransition */
+.orb-returning {
+  transition: transform 0.12s cubic-bezier(0.16, 1, 0.3, 1) !important;
+}
+
 .orb-swapping {
-  transition: transform 0.15s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.3s ease;
+  transition: transform 0.12s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.3s ease;
 }
 
 /* 落下時のバウンスエフェクト */
@@ -234,6 +237,27 @@ body {
   transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.3s ease;
 }
 
+/* ドロップ操作中の全ドロップへの一括 transition ルールは、個別の .orb-swapping クラスによる制御へ移行したため削除しました */
+
+/* 一筆書きの誓約: 通過済みセルのビジュアルインジケーター */
+.one-stroke-tile {
+  position: absolute;
+  pointer-events: none;
+  z-index: 5;
+  border-radius: 8px;
+  background: radial-gradient(circle, rgba(255, 80, 80, 0.35) 0%, rgba(255, 160, 0, 0.15) 60%, transparent 100%);
+  border: 2px solid rgba(255, 120, 0, 0.7);
+  box-shadow: 0 0 10px rgba(255, 100, 0, 0.8), inset 0 0 6px rgba(255, 200, 0, 0.4);
+  animation: one-stroke-pulse 1.2s ease-in-out infinite;
+  box-sizing: border-box;
+}
+
+@keyframes one-stroke-pulse {
+  0%   { box-shadow: 0 0 8px rgba(255, 100, 0, 0.8), inset 0 0 6px rgba(255, 200, 0, 0.4); }
+  50%  { box-shadow: 0 0 18px rgba(255, 180, 0, 1.0), inset 0 0 10px rgba(255, 255, 0, 0.6); }
+  100% { box-shadow: 0 0 8px rgba(255, 100, 0, 0.8), inset 0 0 6px rgba(255, 200, 0, 0.4); }
+}
+
 /* Orb Type Colors */
 .orb-inner {
   width: 100%;
@@ -1313,12 +1337,13 @@ body {
 
 /* パズル盤面セクションのセーフエリア制御 */
 .puzzle-grid-section {
-  padding-bottom: 0px;
+  /* App.jsx側のTailwindでセーフエリアを考慮したpadding-bottomを指定しているため、CSS側での上書きは行いません */
 }
 
 @media (display-mode: standalone) {
   .puzzle-grid-section {
-    padding-bottom: env(safe-area-inset-bottom, 16px);
+    /* ホーム画面追加時（スタンドアロンモード）は、ツールバーがないためセーフエリアにのみ追従させ、不要な隙間（54px）を34pxに削減します */
+    padding-bottom: max(20px, env(safe-area-inset-bottom, 20px)) !important;
   }
 }
 
