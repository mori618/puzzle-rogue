# Puzzle Rogue (パズル・ローグ)

このプロジェクトは、React + Vite で構築されたパズルRPGゲームです。
パズル＆ドラゴンのようなパズル操作感に、ローグライクなトークン・ビルド要素を組み合わせています。

## 🎮 ゲーム仕様 (Specifications)

### 1. 基本ルール
- **パズル方式**: 3つ以上のドロップ（火、水、木、光、闇、回復）を縦または横に揃えて消去します。
- **操作**: ドロップを掴んで自由に移動させ、他のドロップと入れ替えながらコンボを組みます。
- **操作時間**: 初期値は5秒（5000ms）。ドロップを掴んだ瞬間からカウントダウンが始まります。
- **ターン構成**: 各サイクルは3ターンで構成されます。

### 2. サイクルと目標 (Target)
- 各サイクルの目的は、3ターン以内の合計コンボ数が目標（Target Combo）に到達することです。
- クリアすると目標値が上昇し、次のサイクルへ進みます。
- 目標を達成できなかった場合はゲームオーバー（リロード）となります。

### 3. 星 (Stars) とショップ
- **星の獲得**: ターン終了時のコンボ数に応じて星を獲得します。
- **ショップ**: サイクルクリア後にショップがオープンし、獲得した星を使って「トークン」や「エンチャント」を購入できます。

### 4. トークン・システム
装備することで様々なパッシブ効果やスキルを得られます（最大6スロット）。

- **禁忌の儀式 (forbidden)**: 落ちコンがなくなる代わりに、コンボ加算値が大幅に増幅します。
- **黄金の収集者 (collector)**: 星の獲得に必要なコンボ数を短縮します。
- **時の砂 (time_ext)**: 操作時間を延長します。
- **力の鼓動 (power_up)**: コンボ加算に固定値を追加します。
- **次元の再編 (refresh)**: 盤面をリフレッシュします（手数を消費しません、現在は実装準備中）。

### 5. エンチャント
特定のトークンに付与される強力な追加効果です。

- **巨人の領域**: 盤面を 7x6 に拡張します。
- **レベル共鳴**: トークンのレベルに応じてコンボ加算値を乗算します。
- **強欲の輝き**: 現在の所持星数をコンボ加算値に加えます。
- **連鎖の刻印**: コンボ加算値を一律 +2 します。

---

## 🚀 デプロイ方法 (Deployment)

このプロジェクトは `gh-pages` を使用して GitHub Pages に簡単にデプロイできます。

### 1. 前準備
`vite.config.js` の `base` 設定が `'./'` になっていることを確認してください。

### 2. ビルドとデプロイ
以下のコマンドを実行するだけで、プロジェクトがビルドされ、自動的に `gh-pages` ブランチにプッシュされます。

```bash
npm run deploy
```

※ このコマンドは内部で `npm run build` を実行した後、`gh-pages -d dist` を実行します。

### 3. GitHub 側の設定
1. GitHub リポジトリの **Settings > Pages** を開きます。
2. **Build and deployment > Branch** で `gh-pages` ブランチの `/(root)` を選択して保存します。

---

## 🛠 開発環境のセットアップ

### ローカルでの起動
```bash
npm install
npm run dev
```

### ビルド
```bash
npm run build
```
