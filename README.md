# Puzzle Rogue (パズル・ローグ)

このプロジェクトは、React + Vite で構築されたパズルRPGゲームです。
パズル＆ドラゴンのようなパズル操作感に、ローグライクなトークン・ビルド要素を組み合わせています。

## 🎮 ゲーム仕様 (Specifications)

### 1. 基本ルール
- **パズル方式**: 3つ以上のドロップ（火、水、木、光、闇、回復）を縦または横に揃えて消去します。
- **操作**: ドロップを掴んで自由に移動させ、他のドロップと入れ替えながらコンボを組みます。
- **操作時間**: 初期値は5秒（5000ms）。ドロップを掴んだ瞬間からカウントダウンが始まります。
- **ターン構成**: 各サイクルは3ターンで構成されます。

### 2. サイクルと目標 (Target)
- 各サイクルの目的は、3ターン以内の合計コンボ数が目標（Target Combo）に到達することです。
- クリアすると目標値が上昇し、次のサイクルへ進みます。
- 目標を達成できなかった場合はゲームオーバー（リロード）となります。

### 3. 星 (Stars) とショップ
- **星の獲得**: ターン終了時のコンボ数に応じて星を獲得します。
- **ショップ**: サイクルクリア後にショップがオープンし、獲得した星を使って「トークン」や「エンチャント」を購入できます。

### 4. トークン・システム (Tokens)
装備することで様々なアクティブスキルやパッシブ効果を得られます（最大6スロット）。

#### パッシブスキル (Passive)
- **禁忌の儀式 (forbidden)**: 常時落ちコンがなくなる代わりに、コンボ加算値が大幅に増幅します (3倍/4倍/10倍)。
- **黄金の収集者 (collector)**: 星の獲得に必要なコンボ数を短縮します。
- **時の砂 (time_ext)**: 操作時間を延長します (Lvに応じて+1/2/3秒)。
- **力の鼓動 (power_up)**: コンボ加算に固定値を追加します。

#### アクティブスキル (Active)
**変換系**
- **単色変換**: 特定の属性を別の属性に変換します (例: 焔の変換「木→火」など)。
- **複合変換**: 2つの属性を1つの属性に変換します (例: 大地の恵み「火・水→木」など)。
- **横一文字**: 最上段の横一列を特定の色に変化させます。

**陣（ボードチェンジ）系**
- **三色の真理**: 盤面を特定3色（例：火/水/木）のみに変更します。
- **2色陣**: 盤面を特定2色（例：火/水）のみに変更します。大量コンボのチャンス。
- **1色陣 (極致)**: 盤面全てを1色に染め上げます。特殊ボーナス「全画面1色消し(+10コンボ)」が確定します。

**目覚め・結界系**
- **目覚め**: 数ターンの間、特定の色が降りやすくなります (例: 紅蓮の目覚め)。
- **三色の結界**: 3ターンの間、指定された3色しか落ちてこなくなります。落ちコンが繋がりやすくなります。

**その他**
- **次元の再編 (refresh)**: 盤面のドロップを全て入れ替えます（再落下によるマッチングあり）。

### 5. エンチャント (Enchantments)
特定のトークンに付与される強力な追加効果です。ショップで稀に出現するか、付与アイテムとして登場します。

- **巨人の領域 (giant)**: 装備中、盤面が 7x6 マスに拡張されます。
- **レベル共鳴 (resonance)**: トークンのレベルに応じてコンボ加算値を乗算します (xLv)。
- **強欲の輝き (greed)**: 現在の所持星数をコンボ加算値にそのまま加えます。
- **連鎖の刻印 (chain)**: コンボ加算値を一律 +2 します。
- **時の刻印 (extra_turn)**: 目標達成までの猶予ターン数が +1 されます (通常3ターン → 4ターン)。

---

## 🚀 デプロイ方法 (Deployment)

このプロジェクトは `gh-pages` を使用して GitHub Pages に簡単にデプロイできます。

### 1. 前準備
`vite.config.js` の `base` 設定が `'./'` になっていることを確認してください。

### 2. ビルドとデプロイ
以下のコマンドを実行するだけで、プロジェクトがビルドされ、自動的に `gh-pages` ブランチにプッシュされます。

```bash
npm run deploy
```

※ このコマンドは内部で `npm run build` を実行した後、`gh-pages -d dist` を実行します。

### 3. GitHub 側の設定
1. GitHub リポジトリの **Settings > Pages** を開きます。
2. **Build and deployment > Branch** で `gh-pages` ブランチの `/(root)` を選択して保存します。

---

## 🛠 開発環境のセットアップ

### ローカルでの起動
```bash
npm install
npm run dev
```

### ビルド
```bash
npm run build
```
